package geeksforgeeks;

import java.io.InputStreamReader;
import java.util.Scanner;


/*
 * http://www.geeksforgeeks.org/amazon-interview-2/
 * Write your own power function
 * 
 */;
/**
 * Implementation of Amazon Interview_07_02 algorithm/data structure.
 * This class provides methods to solve related problems efficiently.
 *
 * @author Navaneeth Rao
 */
public class AmazonInterview_07_02 {
	/**
	 * Main method to test the functionality of the class with various test cases.
	 *
	 * @param args the array to process
	 */
	public static void main(String[] args) {
		Scanner scanner=new Scanner(new InputStreamReader(System.in));
		int number=Integer.parseInt(scanner.nextLine());
		int power=Integer.parseInt(scanner.nextLine());
		scanner.close();
		int output=myPower(number,power);
	}

	/**
	 * Performs myPower operation.
	 *
	 * @param number the number parameter
	 * @param power the power parameter
	 * @return the computed integer result
	 */
	private static int myPower(int number, int power) {
		
		
		
		return 0;
	}
}